name: CI

on:
  push:
    branches: [master]
  pull_request:

jobs:
  test-and-build:
    runs-on: ubuntu-latest
    env:
      APP_ENV: testing
      CACHE_STORE: array
      DB_CONNECTION: sqlite
      DB_DATABASE: ':memory:'
      MAIL_MAILER: array
      QUEUE_CONNECTION: sync
      SESSION_DRIVER: array

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: '8.2'
          extensions: mbstring, pdo_sqlite
          coverage: none

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: npm

      - name: Install PHP dependencies
        run: composer install --no-interaction --prefer-dist --no-progress

      - name: Install Node dependencies
        run: npm ci

      - name: Prepare application
        run: |
          cp .env.example .env
          php artisan key:generate

      - name: Check code style
        run: ./vendor/bin/pint --test

      - name: Run tests
        run: composer test

      - name: Build frontend
        run: npm run build

  deploy-testing:
    runs-on: ubuntu-latest
    needs: test-and-build
    if: github.event_name == 'push' && github.ref == 'refs/heads/master'
    environment: testing
    env:
      TEST_SERVER_HOST: ${{ secrets.TEST_SERVER_HOST }}
      TEST_SERVER_USER: ${{ secrets.TEST_SERVER_USER }}
      TEST_SERVER_SSH_KEY: ${{ secrets.TEST_SERVER_SSH_KEY }}
      TEST_DEPLOY_PATH: ${{ secrets.TEST_DEPLOY_PATH }}

    steps:
      - name: Validate deployment secrets
        run: |
          test -n "$TEST_SERVER_HOST" || (echo "Missing secret: TEST_SERVER_HOST" && exit 1)
          test -n "$TEST_SERVER_USER" || (echo "Missing secret: TEST_SERVER_USER" && exit 1)
          test -n "$TEST_SERVER_SSH_KEY" || (echo "Missing secret: TEST_SERVER_SSH_KEY" && exit 1)
          test -n "$TEST_DEPLOY_PATH" || (echo "Missing secret: TEST_DEPLOY_PATH" && exit 1)

      - name: Deploy to testing
        uses: appleboy/ssh-action@v1.2.0
        with:
          host: ${{ env.TEST_SERVER_HOST }}
          username: ${{ env.TEST_SERVER_USER }}
          key: ${{ env.TEST_SERVER_SSH_KEY }}
          script: |
            cd "${{ env.TEST_DEPLOY_PATH }}"
            git fetch --prune
            git reset --hard origin/master
            composer install --no-interaction --prefer-dist --no-dev --optimize-autoloader
            npm ci
            npm run build
            php artisan migrate --force
            php artisan config:cache
            php artisan route:cache
